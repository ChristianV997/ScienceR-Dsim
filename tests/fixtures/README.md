# Test Fixtures

This directory stores lightweight text fixtures used by smoke and unit tests.

Rules:
- Keep fixtures small and deterministic.
- Do not add binary blobs, caches, or generated outputs.
- Prefer inline fixture creation in tests when practical.
