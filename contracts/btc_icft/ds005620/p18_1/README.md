# DS005620 P18.1 Artifact Contracts

This directory defines lightweight, stdlib-friendly contract descriptors for the six core
P18.1 DS005620 execution artifacts.

These contract files are consumed by `tools/validate_ds005620_contracts.py` to enforce stable
artifact shapes and key safety invariants without introducing a JSON Schema dependency.
