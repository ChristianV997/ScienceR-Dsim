"""Dual-engine topological simulation package (neuro BIDS EEG + quantum field grids).

All modules are provenance-aware: every field carries a `provenance` tag in
{"real_bids", "synthetic_proxy", "quantum_field"}. No module asserts any
cross-domain physical equivalence -- they compute and report numbers only.
"""
